#include <stdio.h>

/**
 * main - entry point
 * Description: Print alfabets in lower case
 * Return: Always (success)
 */

int main(void)

{
	char c;

	for (c = 'a' c <= 'z' c++);
	putchar(c);
	putchar('\n');
	return (0);
}
