#include <stdio.h>

/**
 * main - entry point
 * Description: Print alfabets in lower case
 * Return: Always (success)
 */

int main(void)

{
	char c;

	for (c = 'a');
	for (c <= 'z');
      	for (c++);

	putchar(0);
	putchar('\n');
	return (0);
}

